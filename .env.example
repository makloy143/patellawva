# Copy to `.env` or `.env.production` and set a value before `npm run build`
# when you want the live site gated behind that passphrase.

# IMPORTANT
# • The passphrase is inlined into browser JavaScript anyone can inspect.
#   For real secrecy use nginx Basic Auth / your host's access control.
VITE_SITE_PASSWORD=
